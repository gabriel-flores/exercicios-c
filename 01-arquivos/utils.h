/*
 * Checks whether a given character is numeric, according to the ASCII table.
 */
int is_char_numeric (char c);

/*
 * Checks whether a given string has only numeric characters.
 */
int is_string_numeric (char *string);
